package com.reactivedemo.reactivedemo;

import reactor.core.publisher.Flux;

public class ReactiveSampleApp {

	/*
	 * public static void main(String[] args) {
	 * 
	 * Flux<String> fluxStr=Flux.just("Hello","good","eve").log();
	 * 
	 * fluxStr.subscribe(s->System.out.println("subs1 "+s));
	 * 
	 * fluxStr.subscribe(s->System.out.println("subs2 "+s)); }
	 */
}
